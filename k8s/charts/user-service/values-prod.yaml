namespace: cloudmart-prod

replicaCount: 2

image:
  tag: latest

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

config:
  awsRegion: us-east-1
  rdsSecretName: cloudmart/prod/rds/user-service

serviceAccount:
  irsaRoleArn: "arn:aws:iam::<AWS_ACCOUNT_ID>:role/cloudmart-user-service-role-prod"
